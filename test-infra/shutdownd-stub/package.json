{
  "name": "flux-shutdownd-stub",
  "version": "1.0.0",
  "private": true,
  "description": "Mock flux-shutdownd (unix-socket JSON-RPC + HTTP control) for the graceful-stop E2E harness. Runs in-container; Node built-ins only.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  }
}
