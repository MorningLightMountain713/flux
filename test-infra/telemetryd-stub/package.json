{
  "name": "flux-telemetryd-stub",
  "version": "1.0.0",
  "private": true,
  "description": "Mock flux-telemetryd (identity-socket client + HTTP control) for the telemetry E2E harness. Runs in-container; Node built-ins only.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  }
}
