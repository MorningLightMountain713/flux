/CloudUI/
# Pinned build artifacts the harness clones and builds from other repos - not this
# repo's source. flux-spec's is a whole JS package, so linting it buried the repo's
# own findings 9:1.
test-infra/*/dist/
/config/userconfig.js
/lib/daemonrpc/
/tests/e2e/
/ZelApps/
/docs/
/dev/
